# HomeFit Next.js App (App Router)

Includes a required `app/layout.jsx` to fix the Next.js error: *page.jsx doesn't have a root layout*.

## Run locally
```bash
npm i
npm run dev
```

## Deploy to Vercel
Push this folder as the **repo root**. Vercel will auto-detect Next.js and build.
